# Swift Package Manager build artifacts
.build/
.swiftpm/
Package.resolved

# Xcode user state
*.xcuserdata/
xcuserdata/
*.xcuserstate

# macOS metadata
.DS_Store

# Generated by `smartapi-bundle openapi` — committed wrappers belong with
# the consumer app, not in this package. The petstore Generated/ tree is
# a demo artifact that gets regenerated on every CI run, so don't track it.
Examples/OpenAPI/Generated/

# Internal launch plan — never commit
LAUNCH.md
